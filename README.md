Bouquet Wines website
=====================

The site content is on `winelist.xml` and is maintained by the business owner.

PHP script `index.php` composes site chrome and invokes XSLs to create HTML page content. 